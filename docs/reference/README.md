# Reference

Authoritative descriptions — API surface, types, schemas. Mirrors the code structure.

For per-crate rustdoc, see [docs.rs](https://docs.rs/llm-repair) for published crates.

Higher-level reference (protocol surface, configuration schemas) lands here as the SDK grows.
