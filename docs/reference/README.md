# Reference

Authoritative descriptions — API surface, types, schemas. Mirrors code structure.

Migrates wholesale from `nsed/docs/agent-sdk/reference/` in Phase 4. Also gains a `protocol/` slice covering the NATS subjects + JWT scopes + JetStream stream layout an SDK agent needs.
