# Tutorials

Learning-by-doing material — single-path lessons with expected output at every step.

Goal: take a new `quorum-rs` user from zero to a working agent.

Planned content (Phase 4):

- `your-first-agent.md` — ~50 line `main.rs` that uses the SDK end-to-end.
