/target
**/*.rs.bk
.DS_Store
*.swp
*.swo

# gh-issue-sync local mirror — issues live on GitHub, only the .sync metadata is tracking state
.issues

# CodeRabbit / qwen review artifacts (local-only)
review*.txt
qwen_review*
.slop/
